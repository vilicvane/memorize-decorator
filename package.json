{
  "name": "memorize-decorator",
  "version": "0.2.4",
  "description": "A simple decorator that memorizes results of methods and getters.",
  "devDependencies": {
  },
  "dependencies": {
    "multikey-map": "^0.2.0"
  }
}
